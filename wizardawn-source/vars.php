<?php

if ( !isset($_SESSION['SESSION_SECTION']) ){ $_SESSION['SESSION_SECTION'] = ""; }
if ( !isset($_SESSION['SCRIPT_FILENAME']) ){ $_SESSION['SCRIPT_FILENAME'] = ""; }
if ( !isset($_SESSION['SESSION_CMM_OSRIC']) ){ $_SESSION['SESSION_CMM_OSRIC'] = ""; }
if ( !isset($_SESSION['SESSION_MM_OSRIC']) ){ $_SESSION['SESSION_MM_OSRIC'] = ""; }
if ( !isset( $mm ) ){ $mm = ""; }
if ( !isset( $menu_on_left ) ){ $menu_on_left = ""; }
if ( !isset( $menu_in_center ) ){ $menu_in_center = ""; }
if ( !isset( $menu_on_right ) ){ $menu_on_right = ""; }
if ( !isset( $menu_section ) ){ $menu_section = ""; }
if ( !isset( $credits ) ){ $credits = ""; }
if ( !isset( $pagame ) ){ $pagame = ""; }
if ( !isset( $lnum ) ){ $lnum = 0; }
if ( !isset( $menu_break ) ){ $menu_break = ""; }
if ( !isset( $hdu ) ){ $hdu = ""; }
if ( !isset( $cash ) ){ $cash = ""; }
if ( !isset( $list ) ){ $list = ""; }
if ( !isset( $clist ) ){ $clist = 0; }
if ( !isset( $cmonster_manual ) ){ $cmonster_manual = ""; }
if ( !isset( $monster_manual ) ){ $monster_manual = ""; }
if ( !isset( $extra_disclaimer ) ){ $extra_disclaimer = ""; }

?>